<Window x:Class="LLM_IDE.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Ra_LLM_IDE" Height="800" Width="1400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Height="25">
            <MenuItem Header="파일(F)"/>
            <MenuItem Header="설정(S)"/>
            <MenuItem Header="계정(_A)">
                <MenuItem x:Name="menuLogin" Header="Gemini 로그인(_L)" Click="menuLogin_Click"/>
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" MinWidth="150" x:Name="ProjectCol"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*" MinWidth="400"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="350" MinWidth="350" MaxWidth="350"/>
            </Grid.ColumnDefinitions>

            <TabControl Grid.Column="0">
                <TabItem Header="세션 목록">
                    <DockPanel Width="200" DockPanel.Dock="Left" Background="#2D2D2D">
                        <Button Content="세션 새로고침" Click="btnRefresh_Click" DockPanel.Dock="Top" Margin="5"/>
                        <ListBox x:Name="SessionList" 
         DisplayMemberPath="Title" 
         SelectionChanged="SessionList_SelectionChanged"
         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
         VirtualizingStackPanel.ScrollUnit="Item">
                        </ListBox>
                    </DockPanel>
                </TabItem>
                <TabItem Header="프로젝트 설정" x:Name="TabProjectSet">
                    <Grid Background="#F9F9F9">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBox x:Name="TxtProjectEditor" 
                     Grid.Row="0"
                     AcceptsReturn="True" 
                     TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto" 
                     Margin="5" 
                     Padding="10"
                     FontSize="12"
                     BorderBrush="#DDDDDD" />

                        <Button x:Name="BtnProjectSave" 
                    Grid.Row="1" 
                    Content="설정 저장" 
                    Height="30" 
                    Margin="5" 
                    Background="#10b981" 
                    Foreground="White" 
                    FontWeight="Bold"
                    Click="BtnProjectSave_Click"/>
                    </Grid>
                </TabItem>
            </TabControl>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="#DDDDDD"/>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ListBox x:Name="LogList" 
         ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
         HorizontalContentAlignment="Stretch"
         ScrollViewer.CanContentScroll="False" 
         VirtualizingStackPanel.IsVirtualizing="True"
         VirtualizingStackPanel.VirtualizationMode="Recycling">

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding TimeStamp}" TextWrapping="Wrap" Foreground="Gray" VerticalAlignment="Center" Padding="5"/>
                                <TextBlock Grid.Column="1" Text="{Binding UserInput}" TextWrapping="Wrap" VerticalAlignment="Center" Padding="5"/>

                                <Border Grid.Column="2" Background="#F0F7FF" CornerRadius="5" Padding="2">
                                    <TextBox Text="{Binding LlmOutput, Mode=OneWay}" 
                             x:Name="rcvMsg"
                             TextWrapping="Wrap" 
                             IsReadOnly="True" 
                             BorderThickness="0" 
                             Background="Transparent"
                             MaxHeight="250" 
                             VerticalScrollBarVisibility="Auto"
                             AcceptsReturn="True"/>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <Button x:Name="BtnSave" Grid.Row="1" Content="Save" Width="80" HorizontalAlignment="Right" Margin="5" Click="BtnSave_Click"/>
            </Grid>

            <GridSplitter Grid.Column="3" HorizontalAlignment="Stretch" Background="#DDDDDD"/>

            <Grid Grid.Column="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBox x:Name="TxtInput" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Margin="10"/>
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
                    <Button x:Name="BtnSend" Content="Send" Width="80" Margin="5" Click="BtnSend_Click"/>
                    <Button x:Name="BtnHandoff" Content="Handoff" Width="80" Margin="5" Click="btnHandoff_Click"/>
                    <Button x:Name="BtnShowWeb" Content="Web" Width="40" Margin="5" Click="BtnShowWeb_Click"/>
                </StackPanel>
            </Grid>
        </Grid>

        <Grid x:Name="WebPopup" Grid.RowSpan="2" Background="#88000000" Visibility="Collapsed">
            <Border Background="White" Margin="50" CornerRadius="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Button Content="닫기" HorizontalAlignment="Right" Margin="10" Click="BtnCloseWeb_Click"/>
                    <wv2:WebView2 x:Name="MainWebView" Grid.Row="1" Source="https://gemini.google.com"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>